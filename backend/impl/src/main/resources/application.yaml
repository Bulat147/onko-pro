spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:onko-db}
    username: postgres
    password: ${DB_PASSWORD:root}
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: liquibase/changelog/changelog-master.yaml

app:
  netlink: ${APP_HOST:http://localhost:8080}

security:
  jwt:
    secret: e0a07206-2d68-44a4-bdc2-4353f5135a32

file:
  photo:
    dir: ${FILE_DIR:files/photo/}
